# Three Agents, Two Mistakes

A talk from posit::conf(2026): *Designing for LLM-Assisted Data Analysis.*

The talk walks through three AI coding/data-analysis agents Posit has
shipped — Positron Assistant, Databot, and Posit Assistant — and the design
lessons learned (and re-learned) along the way:

- **Lesson 1:** every feature you add costs you changeability.
- **Lesson 2:** an agent's autonomy is only as good as what it can access.

## Viewing the slides

The published deck lives at
<https://samclark2015.github.io/conf-talk-2026/>, rebuilt from `main` by the
[publish workflow](.github/workflows/publish.yml).

To build locally (the deck has an executed Python cell, so install the deps
first):

```bash
uv sync
uv run quarto render     # → _site/index.html
uv run quarto preview    # live-reload while editing
```

The QR code on the closing slide is generated by
[`scripts/make_qr.py`](scripts/make_qr.py):

```bash
uv run python scripts/make_qr.py            # defaults to the Pages URL
uv run python scripts/make_qr.py <other-url>
```

## What's in this repo

- [`index.qmd`](index.qmd) — the talk itself
- [`theme.scss`](theme.scss) — Posit brand reveal.js theme
- [`assets/`](assets/) — logos, fonts, and other slide assets
- [`scripts/make_qr.py`](scripts/make_qr.py) — regenerates the closing-slide QR code

## Author

Sam Clark · Posit, PBC
